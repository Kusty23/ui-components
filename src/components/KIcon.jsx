import React, { Component } from "react";

export default class KIcon extends Component {
  render() {
    return (
      <span className="material-symbols-outlined KIcon">{this.props.name}</span>
    );
  }
}
